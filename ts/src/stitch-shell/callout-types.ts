/**
 * Semantic variants for the Stitch Callout primitive. Each variant maps to
 * a token-bound accent color in the framework adapters, so tenant theming
 * flows through without callers having to pick hex values.
 */
export type CalloutVariant = 'info' | 'success' | 'warning' | 'danger';
