# React Buffered SSR Example

From `ts/`:

```bash
npm run example:buffered:serve
```

Open `http://localhost:4172/`.

This path exercises buffered SSR through `FaceApp.handle()` with full HTML document output.
