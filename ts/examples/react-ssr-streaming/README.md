# React Streaming SSR Example

From `ts/`:

```bash
npm run example:streaming:serve
```

Open `http://localhost:4173/`.

This path exercises streaming SSR (headers first, streamed body chunks) through `FaceApp.handle()`.
